# Postgres connection string (Vercel Postgres or Neon).
# On Vercel, adding a Postgres store sets this automatically.
# For local dev, copy this to `.env` and fill in a real connection string.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require"
